import { SceneTrial } from '../scene_trial/model';

export class SceneTrialLocation extends SceneTrial {
    CheckAnswer(): boolean {
        // TODO: compare the current location with the one defined in the solution
        return true;
    }
}
